<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SplashActivity">

    <!-- Black 10% at the top -->

    <!-- Grey 20% at the bottom -->

    <!-- White 70% in the center -->

    <!-- Train Icon (Centered within White Section) -->

    <View
        android:id="@+id/whiteSection"
        android:layout_width="415dp"
        android:layout_height="731dp"
        android:background="@android:color/white"
        app:layout_constraintHeight_percent="0.7"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent="1"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteX="-2dp" />

    <ImageView
        android:id="@+id/trainIcon"
        android:layout_width="200dp"
        android:layout_height="250dp"
        android:contentDescription="Train Icon"
        android:src="@drawable/ic_train"
        app:layout_constraintBottom_toBottomOf="@id/whiteSection"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/whiteSection" />

    <!-- Loading Text (Centered within White Section) -->
    <TextView
        android:id="@+id/loadingText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Loading..."
        android:fontFamily="cursive"
        android:textColor="@android:color/black"
        android:textSize="24sp"
        app:layout_constraintBottom_toBottomOf="@id/whiteSection"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/trainIcon" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="264dp"
        android:layout_height="80dp"
        android:fontFamily="cursive"
        android:text="Smart Rail"
        android:textSize="60sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteY="84dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
